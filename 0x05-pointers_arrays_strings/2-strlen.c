#include "main.h"

/**
 * _strlen - to get lenght of a string
 *
 * @s: string input
 *
 * Return: Length of string
 */

int _strlen(char *s)
{
	int index;

	for (index = 0; s[index] != '\0' ; index++)

	return (index);
}
