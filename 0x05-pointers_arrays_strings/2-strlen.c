#include "main.h"

/**
 * main - check the code
 *
 * int _strlen - to get lenght of a string
 *
 */

int _strlen(char *s)
{
	int index;

	for (index = 0; s[index] != '\0' ; index++)

	return (index);
}
